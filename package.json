{
  "name": "liadias.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "serve": "npx live-server ./dist --host=localhost"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
